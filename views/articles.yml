path: /articles
template: index
context:
  static:
    page_title: articles
  dynamic:
    post_range: "(1, session.query(post).filter(post.is_note == None).filter(post.published != None).order_by(post.published.desc())[0:25])"
