path: /articles
template: index
context:
  static:
    page_title: articles
  dynamic:
    post_range: (1, session.query(post).filter(not post.is_note).filter(post.published != None).order_by(post.published.desc())[0:25])
