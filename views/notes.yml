path: /notes
template: index
context:
  static:
    page_title: notes
  dynamic:
    post_range: (1, session.query(post).filter(post.is_note).filter(post.published != None).order_by(post.published.desc())[0:25])
